init ret_expr;
val result : entero;

entero compute(a: entero, b: entero) {
    {
        regresa (a * a + b * b) / (a + b);
    }
}

arranca {
    result = compute(3, 4);
    dale("compute(3,4) = ", result, "\n");

    result = compute(10, 20);
    dale("compute(10,20) = ", result, "\n");
}
acaba
