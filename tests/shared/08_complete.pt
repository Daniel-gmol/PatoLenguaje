init complete;
val
    x, y, res : entero;
    pi : decimal;

nil imprimeRes(r: entero) {
    val s : entero;
    {
        s = 12 * 10;
        dale("Resultado: ", r * s);
    }
};

entero suma(a: entero, b: entero) {
    {
        regresa (a + b) / 3;
    }
};

arranca {
    x = 10;
    y = 20;
    pi = 3.14;
    res = x + y * 2;

    si (res > 30) {
        dale("Grande");
    } sino {
        dale("Pequeño");
    };

    esperaque (x > 0) \/ {
        x = x - 1;
    };

    imprimeRes(res);
}
acaba
